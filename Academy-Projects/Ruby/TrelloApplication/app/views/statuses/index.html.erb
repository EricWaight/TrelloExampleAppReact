<div class = "header"> 
<img src="../assets/images/header-logo-2x.png" alt="Logo">
</a>
</div>
 <tr><td><%= link_to 'New Status', new_status_path %></td><tr>
<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
  </tr>
 
  <% @statuses.each do |status| %>
    <tr>
      <td><%= status.title %></td>
      <td><%= status.description %></td>
      <td><%= link_to 'Show', status_path(status) %></td>
      <td><%= link_to 'Edit', edit_status_path(status) %></td>
    </tr>
  <% end %>
</table>